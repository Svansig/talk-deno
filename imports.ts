// The import syntax comes from the GO language,
// which also uses URLs (which are mapped to filepaths)
import { success } from 'https://deno.land/x/colorlog/mod.ts';

console.log(success('🦕 Welcome to DENO! 🦕'));
